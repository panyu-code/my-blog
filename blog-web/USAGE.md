# 博客前台系统使用说明

## 📦 项目结构

```
blog-web/
├── src/
│   ├── api/                 # API 接口
│   │   ├── request.js      # Axios 请求封装
│   │   ├── user.js         # 用户相关接口
│   │   ├── article.js      # 文章相关接口
│   │   └── comment.js      # 评论相关接口
│   ├── components/         # 公共组件
│   │   ├── Header.vue      # 头部导航
│   │   ├── Footer.vue      # 底部信息
│   │   └── CommentList.vue # 评论列表
│   ├── router/             # 路由配置
│   │   └── index.js        # 路由定义
│   ├── stores/             # 状态管理
│   │   ├── user.js         # 用户状态
│   │   └── article.js      # 文章状态
│   ├── utils/              # 工具函数
│   │   └── markdown.js     # Markdown 渲染配置
│   ├── views/              # 页面组件
│   │   ├── Layout.vue      # 布局页面
│   │   ├── Home.vue        # 首页
│   │   ├── ArticleDetail.vue  # 文章详情
│   │   ├── Search.vue      # 搜索页面
│   │   ├── Category.vue    # 分类页面
│   │   ├── Tag.vue         # 标签页面
│   │   ├── Login.vue       # 登录页面
│   │   ├── Register.vue    # 注册页面
│   │   ├── About.vue       # 关于页面
│   │   └── NotFound.vue    # 404页面
│   ├── App.vue             # 根组件
│   ├── main.js             # 入口文件
│   └── style.css           # 全局样式
├── .env.example            # 环境变量示例
├── package.json            # 项目依赖
└── vite.config.js          # Vite 配置

```

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

复制 `.env.example` 为 `.env`，并修改配置：

```bash
cp .env.example .env
```

修改 API 地址：
```
VITE_API_BASE_URL=http://your-api-server.com/api
```

### 3. 启动开发服务器

```bash
npm run dev
```

### 4. 构建生产版本

```bash
npm run build
```

### 5. 预览生产构建

```bash
npm run preview
```

## ✨ 功能特性

### 1. 首页（文章列表）
- ✅ 文章卡片展示
- ✅ 分页功能
- ✅ 热门文章推荐
- ✅ 分类导航
- ✅ 标签云
- ✅ 响应式布局

### 2. 文章详情页
- ✅ Markdown 渲染
- ✅ 代码高亮（支持多种语言）
- ✅ 文章点赞
- ✅ 浏览量统计
- ✅ 上一篇/下一篇导航
- ✅ 评论系统集成

### 3. 搜索功能
- ✅ 关键词搜索
- ✅ 搜索结果高亮
- ✅ 热门搜索推荐
- ✅ 搜索历史

### 4. 分类和标签
- ✅ 按分类浏览文章
- ✅ 按标签浏览文章
- ✅ 分类/标签统计

### 5. 评论系统
- ✅ 发表评论
- ✅ 评论回复
- ✅ 评论点赞
- ✅ 评论删除
- ✅ 评论分页

### 6. 用户系统
- ✅ 用户登录
- ✅ 用户注册
- ✅ 用户信息展示
- ✅ 登录状态保持
- ✅ Token 认证

### 7. 响应式设计
- ✅ 移动端适配
- ✅ 平板适配
- ✅ 桌面端优化

## 🛠️ 技术栈

- **Vue 3** - 渐进式 JavaScript 框架
- **Vite** - 下一代前端构建工具
- **Vue Router** - 官方路由管理器
- **Pinia** - Vue 3 状态管理
- **Element Plus** - 基于 Vue 3 的组件库
- **Axios** - HTTP 客户端
- **Markdown-it** - Markdown 解析器
- **Highlight.js** - 代码高亮库
- **Day.js** - 日期处理库

## 📝 开发说明

### API 接口对接

所有 API 接口都在 `src/api/` 目录下定义，需要后端提供以下接口：

#### 用户接口
- `POST /user/login` - 用户登录
- `POST /user/register` - 用户注册
- `GET /user/info` - 获取用户信息
- `PUT /user/info` - 更新用户信息

#### 文章接口
- `GET /article/list` - 获取文章列表
- `GET /article/:id` - 获取文章详情
- `GET /article/search` - 搜索文章
- `POST /article/:id/like` - 文章点赞
- `POST /article/:id/view` - 增加浏览量

#### 分类和标签接口
- `GET /category/list` - 获取分类列表
- `GET /category/:id/articles` - 获取分类下的文章
- `GET /tag/list` - 获取标签列表
- `GET /tag/:id/articles` - 获取标签下的文章

#### 评论接口
- `GET /comment/:articleId/list` - 获取评论列表
- `POST /comment` - 添加评论
- `POST /comment/reply` - 回复评论
- `DELETE /comment/:id` - 删除评论
- `POST /comment/:id/like` - 评论点赞

### 自定义配置

#### 修改主题颜色

在 `src/style.css` 中修改 CSS 变量：

```css
:root {
  --el-color-primary: #409eff;  /* 主色 */
  --el-color-success: #67c23a;  /* 成功色 */
  --el-color-warning: #e6a23c;  /* 警告色 */
  --el-color-danger: #f56c6c;   /* 危险色 */
  --el-color-info: #909399;     /* 信息色 */
}
```

#### 修改代码高亮主题

在 `src/views/ArticleDetail.vue` 中修改导入的样式：

```javascript
import 'highlight.js/styles/github-dark.css'  // 更换为其他主题
```

可选主题：
- `github.css` - GitHub 风格
- `atom-one-dark.css` - Atom Dark
- `monokai.css` - Monokai
- 更多主题见：https://highlightjs.org/static/demo/

## 📄 注意事项

1. **确保后端 API 已启动**
   - 修改 `.env` 文件中的 `VITE_API_BASE_URL`
   - 确保后端接口返回格式符合前端要求

2. **API 数据格式**
   - 所有接口统一返回格式：
   ```json
   {
     "code": 200,
     "message": "success",
     "data": {}
   }
   ```

3. **Token 认证**
   - 登录成功后，Token 会保存在 localStorage
   - 后续请求会自动在 Header 中携带 Token
   - Token 格式：`Authorization: Bearer {token}`

4. **开发调试**
   - 使用 Vue DevTools 进行调试
   - 查看浏览器控制台的网络请求
   - 检查 Pinia Store 的状态变化

## 🎨 自定义开发

### 添加新页面

1. 在 `src/views/` 创建页面组件
2. 在 `src/router/index.js` 添加路由配置
3. 在导航栏添加链接

### 添加新接口

1. 在对应的 API 文件中添加方法
2. 在 Store 中调用接口方法
3. 在组件中使用 Store

## 📞 技术支持

如有问题，请联系：
- Email: blog@example.com
- GitHub Issues: [项目地址]

## 📄 许可证

MIT License
